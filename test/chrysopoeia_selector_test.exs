

defmodule Chrysopoeia.Selector.CSS.Elixir.Test do
  use ExUnit.Case

  #def assert_eq(left, right), do: assert left == right
  #def assert_neq(left, right), do: assert left != right

  #alias Chrysopoeia.Selector.CSS, as: CSS

  #test "#parse - matches an element of type E " do
    ##"p" 
      ##|> CSS.parse("p")
      ##|> assert_eq {"p", nil, nil}
  #end

end

